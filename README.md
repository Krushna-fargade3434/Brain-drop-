# Brain-drop-
A RESTful social posting web app built with Node.js, Express, and EJS. Users can create posts with usernames/content, view all posts in a responsive grid layout, edit existing posts, and delete them. Features in-memory storage with UUID-based IDs, modern gradient UI with animations, and full CRUD operations via method-override middleware.
